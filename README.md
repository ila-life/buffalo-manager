# Buffalo Manager

This project is a Vite-based static site for GitHub Pages.

## Development

```bash
npm install
npm run dev
```

## Build for GitHub Pages

```bash
npm run build
```

The built site will be generated in the `docs/` folder, which GitHub Pages can serve.
